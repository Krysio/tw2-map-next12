{
  "name": "tw2-map",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx cross-env NODE_OPTIONS=--openssl-legacy-provider next dev",
    "build": "npx cross-env NODE_OPTIONS=--openssl-legacy-provider next build",
    "start": "npx cross-env NODE_OPTIONS=--openssl-legacy-provider next start",
    "lint": "npx cross-env NODE_OPTIONS=--openssl-legacy-provider next lint",
    "test": "npx cross-env NODE_OPTIONS=--openssl-legacy-provider jest --watch"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "next": "11.1.2",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/react": "17.0.27",
    "@types/webgl2": "0.0.6",
    "eslint": "^8.40.0",
    "eslint-config-next": "^13.4.1",
    "jest": "^27.2.5",
    "raw-loader": "^4.0.2",
    "ts-jest": "^27.0.5",
    "typescript": "4.4.3"
  }
}
